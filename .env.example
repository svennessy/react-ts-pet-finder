# Vite dev proxy → local pet-db API (default http://localhost:3002)
# VITE_API_PROXY=http://localhost:3002

# Optional: call API directly instead of through the Vite proxy
# VITE_API_BASE_URL=http://localhost:3002
