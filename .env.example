# Upstream Pet DB (used by dev API server to build bbox cache)
NEXT_PUBLIC_API_URL=https://pet-info-db.onrender.com

# Vite dev proxy → local bbox API (default http://localhost:3001)
# VITE_API_PROXY=http://localhost:3001
